{
  "name": "dynamic-survey-system",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "docker compose up --build",
    "dev:clean": "docker compose build --no-cache && docker compose up",
    "build": "npm run build --workspaces"
  }
}
