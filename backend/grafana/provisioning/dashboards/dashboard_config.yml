apiVersion: 1

providers:
  - name: 'Dynamic Survey System Dashboards'
    type: file
    disableDeletion: false
    editable: true
    options:
      # This is the path INSIDE the Grafana container
      path: /etc/grafana/provisioning/dashboards
